spring:
  application:
    name: code-father

server:
  port: 8123
  servlet:
    context-path: /api

# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn

springdoc:
  group-configs:
    group: 'default'
    packages-to-scan: com.lyh.codefather.controller